<div id="error_explanation">
    <h2><%= pluralize(object.errors.count, "error") %> empêche(nt)
    l'objet <%= object.model_name.singular %> d'etre sauvegardé :</h2>
    <ul>
    <% object.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
    </ul>
</div>
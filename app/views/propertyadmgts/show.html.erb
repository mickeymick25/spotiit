<%= render "shared/nav" %>

<h1> Annonce Immobilière : <%= @propertyad.id %></h1>

<%= render partial:'showform', locals: {propertyad:@propertyad} %>
<p>
    <a href="<%= edit_propertyadmgt_path(@propertyad.id) %>" class="btn btn-default">Editer l'annonce</a>
    <a href="<%= propertyadmgt_path(@propertyad.id) %>" data-confirm="Really?" data-method="delete" class="btn btn-default" >Supprimer</a>
</p>
<p>
    <a href="<%= propertyadmgts_path %>"class="btn btn-default" >Annuler</a>
</p>    